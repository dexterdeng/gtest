
- comments.each do |comment|
  .media
    .media-left
      =  link_to ":;" do
        = image_tag asset_path('avt.png'), class: 'media-object', style:'width:40px; height: 40px;'
    .media-body
      -# h4.media-heading
      %div{style: 'display: block;'}= comment.content
      %font{style: "color: #999999; font-size: 13px;"}
        = comment.user
        = '·'
        = l comment.created_at

